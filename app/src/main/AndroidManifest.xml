<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Firebase (Auth, Firestore, Storage, Functions, App Check) needs network. -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- NetworkMonitor watches connectivity so the app can show an offline
         banner, disable "Make My Sketch", and auto-recover when back online. -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- Saving completed artwork to the phone gallery. Scoped MediaStore on
         29+ doesn't need a runtime permission; only legacy 24-28 does. -->
    <uses-permission
        android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />

    <application
        android:name=".ColorMagicKidsApp"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/Theme.ColorMagicKids">

        <!-- AdMob — replace with your real App ID from the AdMob console.
             Using the test app ID here; swap before going live.
             IMPORTANT: This app is child-directed. Rewarded ads are shown only
             to parents (behind the parent gate) to comply with COPPA and the
             Google Play Families policy. -->
        <meta-data
            android:name="com.google.android.gms.ads.APPLICATION_ID"
            android:value="ca-app-pub-3940256099942544~3347511713" />

        <!-- Disable Ad ID collection — required for child-directed apps. -->
        <meta-data
            android:name="com.google.android.gms.ads.DELAY_APP_MEASUREMENT_INIT"
            android:value="true" />

        <!-- Resolve manifest merger conflict between play-services-ads and
             play-services-measurement-api. Both libraries inject an
             android.adservices.AD_SERVICES_CONFIG <property> with different
             resource values. We let play-services-ads win (its resource
             covers the superset of Ad Services configuration needed). -->
        <property
            android:name="android.adservices.AD_SERVICES_CONFIG"
            android:resource="@xml/gma_ad_services_config"
            tools:replace="android:resource" />

        <!-- Kid-safe Analytics: collect product metrics (screen views,
             crashes) but no advertising signals. Disables Ad ID + SSAID
             collection and personalized ads, as required for a child-
             directed app under COPPA / Google's Families policy. -->
        <meta-data
            android:name="google_analytics_adid_collection_enabled"
            android:value="false" />
        <meta-data
            android:name="google_analytics_ssaid_collection_enabled"
            android:value="false" />
        <meta-data
            android:name="google_analytics_default_allow_ad_personalization_signals"
            android:value="false" />
        <meta-data
            android:name="google_analytics_default_allow_ad_user_data"
            android:value="false" />

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.ColorMagicKids">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
