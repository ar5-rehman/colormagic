package com.colormagic.kids.presentation.navigation

// Top-level routes outside the NavigationSuiteScaffold (no bottom nav).
// Splash and Onboarding render full-bleed before the user enters the main app shell.
object RootDestination {
    const val SPLASH = "splash"
    const val ONBOARDING = "onboarding"
    const val MAIN = "main"
}
